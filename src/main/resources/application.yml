server:
  port: 9950
  tomcat:
    uri-encoding: UTF-8

spring:
  http:
    encoding:
      charset: UTF-8
      force: true

swagger:
  docket:
    sso:
      title: SSO模块
      base-package: com.xlaoy.sso

#打开actuator的所有端点
management:
  endpoints:
    web:
      exposure:
        include: "*"